// import {validing} from 'getters';
// import {validTodo, validDone} from 'actions';
export default (function () {
  return {
    vuex: {
      actions: {
      },
      getters: {
      }
    },
    data () {
      return {
      }
    },
    computed: {
    },
    ready () {
    },
    methods: {
    },
    watch: {
    }
  }
})()
